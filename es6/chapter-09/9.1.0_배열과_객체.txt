배열과 마찬가지로 자바스크립트 객체 역시 컨테이너지만, 다음 두 가지 측면에서 배열과 다릅니다.

* 배열은 [값]을 가지며,      각 값에는       [숫자형 인덱스]가 있습니다. 반면
  객체는 [프로퍼티]를 가지며, 각 프로퍼티에는 [문자열]이나 [심볼]이 있습니다.

* 배열에는 순서가 있습니다. 반면 객체는 그런 순서가 보장되지 않습니다.

* 객체의 프로퍼티는 키('문자열'이나 '심볼')와 값으로 구성됩니다.
* 객체의 진짜 특징은 키를 통해 프로퍼티에 접근할 수 있다는 점입니다.